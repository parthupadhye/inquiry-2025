/**
 * <%= className %> Module
 *
 * @generated by inquiry-cli
 */

// Definition and types
export {
  <%= variableName %>Definition,
  <%= variableName %>Steps,
  <%= variableName %>Parameters,
  <%= variableName %>Outputs,
  <%= variableName %>RetryConfig,
<% for (const step of steps) { -%>
  <%= step.variableName %>,
<% } -%>
  type <%= classify(name) %>Input,
  type <%= classify(name) %>Output,
} from './<%= name %>.workflow.js';

<% if (withExecutor) { -%>
// Executor
export {
  <%= className %>Executor,
  execute<%= classify(name) %>,
} from './<%= name %>.executor.js';
<% } -%>
